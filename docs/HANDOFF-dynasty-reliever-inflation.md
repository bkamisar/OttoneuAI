# Handoff: fix future-year (Y1/Y2) pitching SGP inflation

**Diagnose and fix in Fable — surgery on the audited valuation core in `shared.js`; do NOT patch blindly, measure first, then re-verify dynasty across the whole engine (Ohtani/Soto/Skenes must stay intact) and re-check the trade finder.** Symptom: `bid.html` is the first tool to give free agents future-year values, and it surfaced that `calculateDynastyValues` wildly over-credits elite relievers in the Y1/Y2 passes while flattening FA hitters — e.g. Andrés Muñoz shows Y1 value **$151** and dynasty **$236** (should be ~$40-60), Bryan Abreu Y1 **$139**, while FA hitter Corey Seager gets dynasty = his Y0 value (**$18/$18**, i.e. no future contribution at all). The bid *recommendation* is unaffected (it's capped at the Y0 win-now ceiling), so this is purely the dynasty/keeper valuation. Confirmed the Y1 dollar rate is NORMAL (`pitRate $20.91/SGP`, hitShare 39%, replP IP 138/ERA 4.12/SO 124 — all sane), so the inflation is in the per-player **SGP**, not the rate: Muñoz generates ~7.2 pitching SGP in Y1, almost all from the strikeout term. Prime suspect is the SO counting term in `calcPlayerSGP` — `(b.so − repl.so * ipRatio) / sgpDenom['SO']` with `ipRatio = ip/repl.ip` — interacting with how the future-year passes aggregate team SO totals to set `sgpDenom['SO']`; this is the mirror image of the Y0 innings-cap bug we already fixed (Y0 uses `IP_MAX × rosProrationFactor()` as the `selectPitchers` budget and got SO denom 24.9, but the `isFutureYear` path uses the full 1500 budget), so the fix likely lives in how `calculateAllValues` handles pitching aggregation/denominators for `yearKey !== 'proj'`, or in the SO-term proration for high-K/low-IP arms. Reproduce by loading the app (my team is now **"Juan Nation UndeGrom 🫡"**, roster refreshed + renamed since the earlier session), opening `bid.html`, and reading the $DYN column of the top-targets table (relievers dominate, all inflated); the exact per-year breakdown can be measured with an isolated-scope eval of a cache-busted `shared.js` calling `calculateAllValues(cloneY(allRosters,'proj_y1'), cloneEx(fa,'proj_y1'), false, 'proj_y1')` and inspecting the `[values]` line + individual FA `projectedValue`. Committed as a known caveat in `c2982d2`; full model reference is in `MODEL.md`, and the same inflation almost certainly affects **rostered** relievers' dynasty values too (just never spotlighted). All 90 tests currently pass — keep them passing.
